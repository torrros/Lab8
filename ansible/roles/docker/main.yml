- name: Install Docker and Dependencies
  apt:
    name:
      - docker.io
      - python3-docker
    state: present
    update_cache: yes
  register: apt_res
  retries: 10
  delay: 20
  until: apt_res is success

- name: Ensure Docker is running
  service:
    name: docker
    state: started
    enabled: yes
